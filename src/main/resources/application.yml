spring:
  application:
    name: geracao-faturas-cartao-credito-job

  datasource:
    username: username
    password: password
    url: jdbc:mysql://localhost:3306/spring_batch?useSSL=false

  batch:
    chunkSize: 40
    pageSize: 20
    limit: 40
    jdbc:
      initialize-schema: always
